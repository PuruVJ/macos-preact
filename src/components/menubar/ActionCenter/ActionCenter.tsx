import React, { FC } from 'react';
import { MenuShell } from '../MenuShell';

export const ActionCenter: FC<{}> = ({}) => {
  return (
    <MenuShell>
      <button>Hello</button>
    </MenuShell>
  );
};
